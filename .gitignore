node_modules/

# electron-builder output. dist/BW Ladder Review.exe is committed on purpose; everything else
# in there is intermediate.
dist/win-unpacked/
dist/builder-debug.yml
dist/*.blockmap
dist/*.yml

# Native build intermediates
native/build/
*.obj
*.pdb
*.ilk

# Written at runtime
match-history.json
config.json
